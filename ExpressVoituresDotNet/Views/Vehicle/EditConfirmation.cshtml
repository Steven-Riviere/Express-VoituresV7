@model ExpressVoituresDotNet.Models.ViewModels.VehicleViewModel

@{
    ViewData["Title"] = "Voiture modifiée";
}

<div class="container text-center mt-5">
    <h2 class="text-muted">Merci !</h2>
    <p>Votre voiture <strong>@Model.Label</strong> a bien été modifiée !</p>
    <a href="@Url.Action("Index", "Vehicle", new { id = Model.Id })" class="btn btn-custom">Retour à l'accueil</a>
</div>
